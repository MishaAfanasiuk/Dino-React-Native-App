import {StyleSheet} from "react-native";

export const homeStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0a4',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

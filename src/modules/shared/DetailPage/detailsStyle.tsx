import {StyleSheet} from "react-native";

export const detailStyles = StyleSheet.create({
  image: {
    backgroundColor: '#756',
    height: 210,
    width: '100%',
  },
  wrapper: {
    alignItems: 'center'
  }
});

import {StyleSheet} from "react-native";

export const eventTimeStyles = StyleSheet.create({
  wrapper: {
    margin: 10,
    flexDirection: 'row',
  },
  when: {
    opacity: 0.6,
    fontSize: 20,
    fontWeight: 'bold',
  },
  time: {
    fontSize: 20,
    fontWeight: 'bold',
  }
});

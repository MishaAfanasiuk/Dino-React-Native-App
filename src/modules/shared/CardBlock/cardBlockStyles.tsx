import {StyleSheet} from "react-native";

export const cardBlockStyles = StyleSheet.create({
  scrollWrapper: {
    height: '100%'
  },
  wrapper: {
    alignItems: 'center',
    zIndex: 1000,
  },
});

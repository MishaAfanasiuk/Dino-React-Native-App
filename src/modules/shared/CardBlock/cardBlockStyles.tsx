import {StyleSheet} from "react-native";

export const cardBlockStyles = StyleSheet.create({
  scrollWrapper: {
    paddingBottom: 60,
  },
  wrapper: {
    alignItems: 'center',
    zIndex: 1000,
    paddingBottom: 60,
  },
});

import {StyleSheet} from "react-native";

export const postTimeStyles = StyleSheet.create({
  wrapper: {
    width: '100%',
    marginBottom: 20,
    marginTop: 30,
    textAlign: 'right',
    opacity: 0.5,
  },
});
